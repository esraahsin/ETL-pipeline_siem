# Kibana SIEM Dashboard Export
#
# This file is intended to hold the NDJSON export of the Kibana SIEM dashboards
# generated after the platform is deployed and configured.
#
# To export dashboards from a running Kibana instance, run:
#
#   curl -X POST "http://localhost:5601/api/saved_objects/_export" \
#     -H "kbn-xsrf: true" \
#     -H "Content-Type: application/json" \
#     -d '{"type": "dashboard", "includeReferencesDeep": true}' \
#     > kibana/siem_dashboard.ndjson
#
# Dashboards included (to be exported):
#   - SIEM Overview (top 10 source IPs, event timeline, severity distribution)
#   - Firewall Analysis (allowed/blocked traffic, top ports)
#   - Web Server Security (status codes, top URIs, suspicious user-agents)
#   - IDS Alerts (Suricata signatures, severity heatmap)
#   - GeoIP Map (source IP geolocation)
#   - Windows Events (logon failures, privileged access)
